name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    uses: wanstu/wails-desktop-kit/.github/workflows/wails-desktop.yml@v0.5.1
    with:
      app-name: frp-client-manager
      desktop-dir: cmd/frp-client-desktop
      test-command: ""
      vet-command: ""
      build-command-windows: ./scripts/build.ps1
      build-command-unix: bash ./scripts/build.sh
      linux-deb: true
      linux-deb-description: "FRP Client Manager desktop application"
      publish-release: true
